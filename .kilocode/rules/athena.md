# Kilo Code Agent Rules

> Auto-generated by `athena init --ide kilocode`

---

## Oscar's Context (Session 05)

You are an AI agent operating within an Athena workspace. Always check `.framework/modules/Core_Identity.md` for your operating principles.

### About This Workspace

- **Project**: PJ - VA Benefits Digital Brain
- **Purpose**: AI assistant for veterans' VA claims
- **Non-negotiable**: Zero hallucinations — all legal citations must be real and verified

### Oscar's Rules for Kilo Code

1. **ALWAYS use Windows/PowerShell commands** — Never use Linux commands (bash, rm, cp, mv, sed, grep, cat, etc.). This is Windows 11 with VS Code + Kilo Code.

2. **Explain step-by-step** — Oscar is learning and wants to eventually do more independently.

3. **Checkpoint frequently** — ADHD-friendly; context loss is stressful. **Always suggest/checkpoint after large subtasks.**

4. **Document everything** — If Oscar has to come back weeks/months later, they shouldn't start from scratch.

5. **Ask before git commands** — Oscar is still learning git.

6. **No "ship it" without full explanation** — Break it down first.

7. **Privacy first** — Never commit/push files with PII, API keys, or sensitive info. Always review diffs before pushing.

---

## Key Directories

- `.framework/` — Your identity and laws (read this first)
- `.context/` — Session logs, user profile, memories (check for prior context)
- `.agent/workflows/` — Available commands (start.md, end.md, save.md)
- `PR_ideas/` — Oscar's documentation of installation journey

---

## Boot Command

Run `athena` to boot the session system, or manually read the latest session log.
On session start — run `/start` workflow to load the user profile.

---

## Session Discipline

1. On session start — run `/start` workflow
2. During work — periodically run `/save` to checkpoint
3. On session end — run `/end` workflow

---

## Slash Commands

| Command | Action |
|---------|--------|
| `/start` | Boot session — loads identity + JIT routing |
| `/end` | Close session — finalize, sync, commit |
| `/save` | Quicksave checkpoint to session log |
| `/think` | Escalate reasoning depth |
| `/ultrathink` | Maximum depth + full context stack |

---

## Last Updated

- **2026-03-03** — Added Oscar's Kilo Code rules (Step 1 of iterative customization)
